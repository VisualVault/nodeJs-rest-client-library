/**
 * VV.Form.Global.IsMapLoaded
 * Parameters: 0
 * Extracted from WADNR (vv5dev/fpOnline) on 2026-04-08
 */
function () {
 return !!(VV.Form.Global.preFetchedWebMapResponse && VV.Form.Global.mapState);
}
